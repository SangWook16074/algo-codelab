# 3. 정렬 활용

정렬은 그 자체가 목적이기보다 **거의 모든 문제의 전처리**입니다. 투 포인터·이진 탐색·그리디가 전부 "정렬된 상태"를 전제로 하죠. `sorted`와 `key`를 자유자재로 쓰는 게 목표입니다.

> 🚧 이 챕터는 준비 중입니다. 다룰 내용: `sorted` vs `.sort()`, `key`로 정렬 기준 바꾸기, 여러 기준(튜플) 정렬, `reverse`, 정렬이 왜 다른 기법의 밑밥이 되는지.
