# 7. 이진 탐색

정렬된 데이터에서 **탐색 범위를 절반씩 줄여** O(log n)에 원하는 값을 찾습니다. "선형으로 훑기"에서 "반씩 버리기"로의 발상 전환이자, 파라메트릭 서치의 토대입니다.

> 🚧 이 챕터는 준비 중입니다. 다룰 내용: `left/right/mid` 기본형, 경계 조건(무한 루프 조심), `bisect` 모듈, lower/upper bound.
